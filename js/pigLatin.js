exports.translate = function(string) {
  /**
   * Receive a word or phrase as an argument to a function
   * Return either the word or the whole phrase in pig latin as a string
   * Pig Latin rules:
   */

  let vowelArr = ["a", "e", "i", "o", "u", "y"]

  let outputString = "";
  return outputString;
};

console.log(this.translate("test"))